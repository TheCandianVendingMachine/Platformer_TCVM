// states.hpp
// enum of all game states. Includes all states as well for easy includes
#pragma once

enum states
    {
        ALL_STATES,
        GAME_STATE,
        MENU_STATE,
        LEVEL_EDITOR_STATE,
        NO_STATE,
    };